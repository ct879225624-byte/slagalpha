# 开发恢复记录 — 2026-08-31

## 当前状态

- P0–P8、P9.1、P9.2a 完成；P9.2b 真实 DEV 研究因历史规则缺失阻断。
- 已自动完成参数计划、规则取证工具、DEV 运行记录、只读前检及本地环境版本锁；不能越过阻断执行回测。
- 进度口径校正：P0–P8 是 9 个阶段，总阶段为 14，故为 9/14（约 64%）。此前
  10/14 为计数错误，不是已完成工作减少；P9 当前完成 1/4。
- 没有访问账户、API Key、私有接口、真实下单或执行锁定测试。
- 用户已批准整理 Git 跟踪范围并创建首次本地提交；方案见 `docs/git-baseline.md`。
  实际提交及干净状态以验收后生成的 `artifacts/git-baseline-2026-08-31.json` 为准。

## 本次交付

- 固定 DEV/VALIDATION/LOCKED_TEST 为 548/274/274 天。
- 输入审计验证全部 1,096 个真实快照与 P8 版本序列一致。
- 32,880 个成员日全部因历史规则未验证而阻断，0 个成员日可执行。
- LOCKED_TEST 基础访问保护无布尔绕过入口。
- split：`b262a24e59f69d7887e8bd5805eb9f11c4fcaef6611c8cd7480d5a2ca89027ca`
- audit：`d58f1cc2adcad91ebbc33bc3864e1f78388b2c9c8f6adb83f1b88f5b3d99e864`
- 默认敏感度计划：10 个参数组、3 种成本、30 个评估配置，仅 DEV；没有执行收益研究。
- plan：`c41e2771a8ca526e4c8ffa09a863b078e300fc7332b9090461e11e1c1f2b5ff9`
- 增加 split/audit 内容完整性校验，防止修改 readiness 后继续沿用旧哈希。
- 官方/提供方文档核验结果见 `docs/p9-historical-rule-evidence-gap.md`。
- 继续开发交付：DEV 取证缺口清单共 248 个合约、2,564 段连续缺口、16,440 个
  受阻成员日；仅 00:15 UTC 门控计数，不冒充整日/持仓期规则覆盖。
- gaps：`78b11fb3e35e22ae90108f8be2e512342483022aae938dd02a983b2c1664d808`。
- 本地证据验收器校验哈希、身份/filter、时间与路径边界；输出最多 READY_FOR_REVIEW，
  不升级 VERIFIED、不改注册表、不授权研究。操作说明见 `docs/p9-rule-evidence-intake.md`。
- 真实当前 BTC 快照不能通过 DEV 历史区间验收；无效声明仅在内存构造用于反例验证。
- intake 反例报告：`eaac96511c1f737f319161bd89e49885d84e324b245ddfc9fbf1e9bb72384d22`。
- 本轮继续开发：DEV RunManifest 与不可覆盖的结果保存；接入现有 P7 合成重放测试，
  校验完整字段/哈希、开发试验标签、并发幂等和中断恢复，不生成真实策略报告。
- 只读前检核对 P9 工件与实际 Git 状态；不 add/commit，异常时不默认干净。
- `requirements.lock` 固定当前 CPython 3.12.13 / win32 / AMD64 的 29 个依赖版本，
  含环境一致性验证，未安装或升级包；不是 Ubuntu 部署锁，未验证 wheel/source 工件哈希。
- lock hash：`8c3d1ef3887544516ac06fa3efe7f9bcfc2b81b1f56b1da267cafa6a24574ee7`。
- 当前 preflight v0.2.0：`759e25c55cba9e73d25a8a69e894057a9bfba27e76ce7c0de26d60e648f7b044`。
- 上述提交前报告有历史规则未验证、无 Git 提交、工作区 dirty 三项阻断；环境锁已补齐。
  Git 提交后的新检查报告记入本地基线回执，旧报告不覆盖。
- 溯源记录、限制和命令说明见 `docs/p9-run-provenance.md`。

## 验证

- 全仓 pytest：328 passed（本轮新增 64 项；上一轮 264 项，合成证据仅用于测试）。
- Ruff：通过。
- Mypy：88 个源码/测试/脚本文件通过。
- `pip check` 通过；前检确认锁文件与实际环境一致，两次运行哈希相同。
- gaps 和 intake 反例各重复运行，哈希一致；P9.1/P9.2a 原工件重跑未变。

## 下一边界

需要可核验的历史 tick/step 区间材料后才能开始真实 DEV 研究；不能用当前规则替代。
Tardis 路径需要订阅/密钥，且 tick/step 变更不保证完整，未自动接入。Git 基线已获授权，
验收完成后会有真实提交与干净工作区记录；不存在本地基线回执时不得假定已完成提交。

收到真实材料后先运行本地证据验收、人工复核来源与连续性，再独立生成新规则注册表
并重跑审计。不重复扩大参数搜索、不启动 P9.3/锁定测试，也不把准备工作计作 P9.2 完成。
RunManifest 与前检不是授权器：正式参数版本的内容/计划绑定和完整运行输入核验仍待
后续执行器实现，不能把字段格式有效或前检通过当成可以晋级。
基线范围：代码/测试/脚本/文档/配置和 29 份关键清单，批量行情及生成记录仍保留本地。
本次不推送远程，不删除任何行情或历史报告。Git 基线不会解除历史规则证据缺口。
