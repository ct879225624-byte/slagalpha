# 开发续接记录：2026-09-02

用户授权：按默认方案逐项完成、验证、汇报后自动进入下一项；不重复询问普通工程选择。
真实下单、生产、账户/凭证和付费资源操作仍不在本轮范围。

## 本轮新增完成

1. `c94afbd`：27 个多周期失败 ZIP 的真实缺口审计。
   有限窗口与直接重叠均为 0，但 18 个文件存在递归 ATR 历史依赖；不放宽门禁。
2. `f28f2bf`：将缺口审计绑定到内容和语义门禁，传播诊断，保留旧报告兼容性。
3. `b1c9fa9`：29 个当前锁定版本 wheel 的元数据、平台和逐文件哈希清单。
   离线强制哈希 dry-run 通过；未安装、升级或修改原环境锁。

所有提交仅在本地；wheel 共 78,976,028 bytes，保存在项目已忽略的数据目录中，不进入 Git。
原始市场数据与冻结策略/split/audit/plan 未改动，没有策略收益结果。

验证：385 passed，1 skipped（Windows 不允许创建符号链接）；Ruff、mypy 105 文件、
pip check 均通过。新增测试使用显式合成 ZIP/wheel，不混入真实数据目录。

## 当前证据

- 缺口审计：`9fdfe51b0a209451b2bae612f427ba33702b8225b71b03211372f0c986c1a7fc`
- 依赖工件：`060d25d955664a803bbdc39b1eecbd466cb57a4a9c5ee278c9c15cb4358bc567`
- 内容门禁：`b1175c2a36c7baac885b160df48a57981bfa2ac05de5576883480baa2e9c8c6f`
- 语义门禁：`095a2f2e7d33e2e5247a5656a919c9fec15b38daa4e86e320331b0dd4f9f20fd`

内容已验证 12 类必需工件及 1 份附加诊断；只缺 1m 和 Funding。
语义验证通过 11 类，deferred 为多周期 Candle、1m、Funding。所有报告仍不授权研究。

## 自动进入下一项后的边界核验

只读遍历本地数据目录，未发现 1m 路径或 Funding/replay 工件。更关键的不是文件数量：
`p9-research-contract.md` 要求按已接受 replay 请求获取这些数据，不预取全历史。
当前 DEV 16,440 个成员日全部因历史规则未验证而不可执行，因此不产生伪造的 accepted
replay 请求，也不把“无可执行请求”当成空数据集已通过验收。

默认后续顺序：

1. 使用既有证据清单补齐可审核的历史合约规则，先经过本地 intake 与人工审核边界；
   不把今日 tick/step 回填历史，不自动改为 VERIFIED。
2. 在冻结 DEV 范围内验证实际输入依赖和递归指标种子边界；全时期缺口不直接当作
   DEV 失败，也不能因此忽略实际执行依赖。
3. 实现并验证按 accepted replay 请求限定的 1m/Funding 工件契约，再按请求获取数据；
   缺失事件、区间、来源和哈希一律失败关闭。
4. 上述门禁齐备后才执行 DEV 默认候选；验证集和锁定集仍按原阶段顺序进行。

目前没有越过数据证据/审核边界，没有启动全历史 1m/Funding 下载。
整体工程进度保持 P0–P8 完成、P9 1/4、9/14（约 64%）；不把准备工作计为研究完成。

## 后续自动执行批次：请求级数据契约

在没有真实历史规则材料的情况下，本批次只继续建设可独立验证的输入契约，不伪造取证。

- `f1969c1`：DEV 数据请求范围。默认 526 根 1m，覆盖最后可能入场后的 TIME_EXIT；
  全窗口须位于 DEV 内，并有单条 VERIFIED、固定 tick 的历史规则覆盖。
- `b051b44`：保存的 REST 响应校验。1m 使用严格分钟网格与现有 Decimal 规范化器；
  Funding 不硬编码频率，拒绝截断、缺数值、错误 symbol/时间和未经解释的空历史。
- `b7fa218`：同请求联合加载。先重验原计划/参数/Universe/规则，再读取和重验原始响应；
  合成 P7 测试验证 TP2、最晚 TIME_EXIT 与 Funding 接入。
- 总运行门禁现能识别单请求 schema，但仍要求 `REQUEST_SET_COVERAGE_REQUIRED`；
  单份合格样本不代表完整 DEV 请求集合，不清空真实阻断。

验证更新：455 passed、1 skipped（同一 Windows symlink 权限限制）；Ruff、mypy 111 文件、
pip check 通过。真实语义报告仍为
`095a2f2e7d33e2e5247a5656a919c9fec15b38daa4e86e320331b0dd4f9f20fd`。
本批次没有生成真实 replay 请求、下载行情、消费锁定测试或进行实盘操作。

详细接口与复现入口见 `docs/p9-replay-data-contract.md`；当前以 Python 函数为入口，
没有自动下载器/执行器。下一开发任务是完整 DEV 请求集合与其上游扫描证据的绑定，
再接受对应的多份 1m/Funding 工件；不能用人工挑选的单笔或空集合宣称研究完成。
实际生成请求与运行研究仍需先解决历史规则证据及多周期依赖/种子边界。

## 扫描覆盖准备：第 1 项

新增 DEV 应扫描清单，绑定冻结 split/计划/参数、完整 Universe 日期/version 序列，
以及每份快照完整内容哈希。严格按 00:15 UTC 池切换；最后一天止于 DEV 午夜。
清单不是扫描结果，保留原计划 blockers，授权与执行标记始终 false。

真实元数据两次生成相同清单：
`7a38b139b799572693a0e095f4f0ec3fa560626625da8fe2f601c73afe943706`，
548 天、1,578,210 个应扫描记录位。原始行情未读取或修改，没有产生实际信号。
新增 18 项测试，全仓 473 passed、1 skipped；Ruff、mypy 114 文件、pip check 通过。
接口和边界见 `docs/p9-scan-request-set-contract.md`。
下一项逐日扫描证据与完整请求集合绑定；整体仍为 9/14、P9 1/4。

## 扫描覆盖准备：第 2 项

第 1 项已本地提交 `33f9019`。新增逐日逐位扫描声明与 accepted 请求一一绑定：
先重建扫描清单、重算当前规则/快照审计，再逐日验完整覆盖，最后生成所有 accepted
请求。缺项、同数量重复、BLOCKED、重复 signal ID、跨 DEV 计划整组拒绝。
重验原始每日证据可识别手选请求子集，以及不产生信号的记录来源变化。

每日来源哈希尚未被实际策略重算验证，因此明确保留 `strategy_evidence_verified=false`，
不把声明当执行证明，不允许空请求集合解除门禁。测试全部是明确合成数据。
新增 30 项；全仓 503 passed、1 skipped，Ruff、mypy 116 文件、pip check 通过。
无真实扫描/请求/下载，整体仍为 9/14（约 64%）、P9 1/4。
下一项为完整请求集合的逐请求 1m/Funding 工件配对和原始字节复验。
