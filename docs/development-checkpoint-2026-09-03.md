# 开发续接记录：2026-09-03

用户要求：使用默认方案顺序开发，完成一项后自动进入下一项，每项汇报整体进度。
不重复询问普通工程选择；付费/凭证、历史规则审核、实盘等边界不因自动继续而改变。

## 第 1 项：扫描器原始 Candle 输入复验

新增 `research/candle_inputs.py`，校验下载/规范化凭证与固定归档身份，从原始 ZIP
重算完整 Candle，再逐字段核对 Parquet schema、元数据、值和入库时间。
防止同时伪造 Parquet 与其摘要后被接受；不把 Decimal 小数位差异当作值差异。
只访问项目内标准路径，拒绝符号链接和路径逃逸。原始数据未修改。

新增 26 项合成测试；本地真实 BTCUSDT 2024-01 四周期 3,937 行复验成功，重复报告哈希：
`f35e1e293a05278f0d605518ce91923f7e9451a96a266eb4625f2c824a62898e`。
全仓 557 passed、1 skipped（Windows symlink 权限），Ruff、mypy 121 文件、pip check 通过。

下一项：将完整连续历史前缀与扫描位绑定，严格控制多周期已收盘可见性并保留种子未验标记。
接口和限制见 `docs/p9-scanner-input-contract.md`。
整体保持 P0–P8 完成、P9 1/4、9/14（约 64%），未运行真实策略或实盘。

## 第 2 项：扫描位绑定的连续历史前缀

第 1 项本地提交 `a1b457b`。新增 `research/candle_history.py`，严格控制四周期最新收盘
可见边界、完整月份和分钟网格，将前缀内容绑定到 DEV 扫描位与 Universe。
缺月、乱序、首尾和跨月缺 K 线拒绝；允许前置 warm-up，但不把声明起点标成已验证种子。
保存的前缀须重新读取原始文件，跨参数版本绑定在文件读取前拒绝。

过程中发现并修复原直接规范化入口的幂等问题：复用既有 Parquet 时，新清单曾写入当前
时间而非文件原入库时间。现保留原时间，新文件使用毫秒时间，原文件没有被改写。
增强既有回归，验证重复调用返回同文件与同清单。

新增 25 项；全仓 582 passed、1 skipped，Ruff、mypy 123 文件、pip check 通过。
真实四周期 pilot 报告哈希未变。下一项重算指标和 Pivot/Zone 的逐时点证据，先用合成
数据验证因果性与参数绑定，仍不运行真实策略、不放宽历史种子与规则门禁。
整体仍为 9/14（约 64%）、P9 1/4。

## 第 3 项：P3 逐时点特征重算

第 2 项本地提交 `bf145e1`。新增 `research/scan_features.py`，先校验计划/候选/扫描绑定，
再重读原始历史并计算指标、候选 Pivot 参数和 Zone。保存完整输入、参数、版本和输出摘要，
复用必须重算；自行重哈希的伪造指标或删减结构无法通过。
未来行改动不影响此前计算值，但月度源文件身份变化仍会改变输入凭证哈希。

22 项合成测试通过；全仓 604 passed、1 skipped，Ruff、mypy 125 文件、pip check 通过。
指标 NaN warm-up 显式保留，不补零；历史起点未审核、策略未运行和研究未授权标记保留。
下一项是绑定四周期特征并重算 P4 Setup，区分 NOT_READY 与合法短路，不伪造 NO_SIGNAL。
没有运行真实行情 P3/P4/P7 或下载新数据；整体 9/14（约 64%）、P9 1/4。

## 第 4 项：四周期 P4 重算

第 3 项本地提交 `2e16043`。新增 `research/scan_setup.py`，绑定同一记录位的全部四周期
特征，重验来源/P3 后计算 P4。EVALUATED 与 NOT_READY 明确分离，非法输入直接拒绝。
历史不足不被计作 NO_SIGNAL；P4 短路仍不绕过绑定来源的真实性/字节复验。

15 项合成测试通过，覆盖中性、压缩、日线阻断和有效回踩。确认 4H 的 12 根完整 SMA180
判断至少需 191 根初始历史，而 185 根只够单个 SMA180 斜率参考，不足以令 P4 全部预热；
ATR 起点充分性仍另行审核，没有放宽任何门禁。
全仓 619 passed、1 skipped，Ruff、mypy 127 文件、pip check 通过。
下一项：从当前 1H 指标冻结回踩区域，接 P5 触发证据并验证来源重算。
全部策略样例仍为合成测试，整体仍为 9/14（约 64%）、P9 1/4。

## 第 5 项：P5 触发证据重算

第 4 项本地提交 `6393e7d`。新增 `research/scan_trigger.py`，从同一时点 1H 指标推导
SHALLOW/STANDARD 的冻结回踩区域，再将重验的 P4 与 15m P3 接入现有 P5。
SKIPPED_SETUP、NOT_READY、已评估但未触发保持独立；模型声明不能代替源数据重算。

10 项合成测试验证原始 ZIP → P3 → P4 → MA Reclaim 的正向链路，以及来源/区域/结果防篡改。
全仓 629 passed、1 skipped，Ruff、mypy 129 文件、pip check 通过。
下一项将 P5 接入历史规则约束下的 P6 Entry/Stop/TP，再复用有限 DEV 请求契约。
仍无真实行情策略运行、网络下载或研究授权；整体 9/14（约 64%）、P9 1/4。

## 第 6 项：P6 与有限 DEV 请求

第 5 项本地提交 `5414d53`。新增 `research/scan_trade_plan.py`，将源数据重算的 P5
接入历史规则约束下的 Entry/Stop/TP，再生成有限 DEV 数据请求。历史规则与 Universe
在读取行情前检查；accepted plan 的整段请求仍须落在 DEV 且由 VERIFIED 规则覆盖。
无触发、历史不足、Entry/Stop 拒绝和 TP 拒绝单独保留；拒绝的计划不产生数据请求。

新增 16 项合成测试，含原始四周期行情到 526 根 1m 请求的正向链路、止损/障碍拒绝、
跨 DEV、规则失效和伪造结果复验。全仓 645 passed、1 skipped，Ruff、mypy 131 文件、
pip check 通过。截至本项新增 114 项测试；真实四周期 pilot 原始输入未改写。
下一项：把逐位计算结果转换为现有扫描记录，历史不足不得伪装成 NO_SIGNAL。
当前仍不是全 DEV 调度器，没有真实策略运行、数据下载或种子审核。
整体保持 9/14（约 64%）、P9 1/4。

## 第 7 项：逐位扫描记录接线

第 6 项本地提交 `0ed092a`。新增 `research/scan_records.py`，先重验原始来源与
P3–P6，再转换为现有扫描记录；保存完整 P6 证据引用与规范阶段原因。
P4/P5 历史不足均为 BLOCKED，合法短路/无触发为 NO_SIGNAL，价格计划拒绝为
REJECTED_PLAN。来源变化、规则退回未验证、伪造记录均拒绝，不自动吞错。

新增 17 项测试；全仓 662 passed、1 skipped（Windows 符号链接权限），Ruff、
mypy 133 文件、pip check、diff 空白检查通过。本批七项任务累计新增 131 项测试。
已有每日聚合器尚不自动重验逐位来源，未审核标记保持不变，不能宣称全 DEV 扫描完成。

## 续接输入边界复核

真实四周期 3,937 行复验报告仍为
`f35e1e293a05278f0d605518ce91923f7e9451a96a266eb4625f2c824a62898e`；
总语义报告仍为 `095a2f2e7d33e2e5247a5656a919c9fec15b38daa4e86e320331b0dd4f9f20fd`，
预期退出 1，未放宽规则、递归依赖、多周期不完整、1m/Funding 缺失等阻断。

本地规则验收目录只有既有反例报告 `eaac9651…`，状态 BLOCKED、UNVERIFIED，注册表
没有改动。重新计算真实 DEV 规则缺口仍为 16,440 个受阻成员日、0 个合格成员日，
248 个合约、2,564 段缺口；报告 `78b11fb3e35e22ae90108f8be2e512342483022aae938dd02a983b2c1664d808`。
这些是证据需求而非日内规则连续性证明。冻结策略原始字节和环境锁哈希保持不变。

下一实质研究步骤仍需要可审核的历史规则及实际递归种子材料；不能由默认方案代替审核。
后续真实逐日调度、来源覆盖证明、accepted 请求数据采集和 P7 运行尚未完成。
全部正向策略验证使用明确合成 ZIP/Parquet 和 VERIFIED 规则测试夹具；没有对真实市场
运行策略、接入账户、消耗 LOCKED_TEST 或下单。整体 9/14（约 64%）、P9 1/4。

## 第二批第 1 项（累计第 8 项）：整日来源接线

续接基线为 `50cc977`，工作区干净。新增 `research/scan_days.py`：重建清单和规则审计
后逐位重验，拒绝来源缺位/重复/错序/额外项；午夜归属旧 Universe，空池严格要求空流。
BLOCKED 不转换为 NO_SIGNAL；整日复用重算全部记录，包括无信号原因。

17 项新增测试；全量 679 passed、1 skipped，Ruff、mypy 135 文件、pip check 通过。调度测试使用显式
逐位接口 mock，并用最后一位完整合成 ZIP/Parquet 重算验证接线，未声称真实整日扫描。
下一项是不可变来源保存与恢复复验，再接完整请求集合。整体仍为约 64%、P9 1/4。

## 第二批第 2 项（累计第 9 项）：来源保存与恢复

上一项提交 `7792226`。新增 `research/scan_storage.py`：来源先发布、整日完成记录后
发布；中断重试可复用一致文件，但不修补已完成后丢失的来源，不覆盖变动字节。
恢复必须重建上下文、读取原始 ZIP/Parquet 并重算逐位结果；JSON 自洽不等于源数据有效。
规范 JSON/哈希/文件名、逐层 symlink/junction 路径约束保留，源读取接口明确为声明读取。

新增 21 项测试；全量 700 passed、1 skipped，Ruff、mypy 137 文件、pip check 通过。
持久化测试明确 mock 整日重算边界；完整调度/原始数据链路由对应模块测试覆盖。
下一项将恢复后的来源日接入完整 DEV 请求集合；整体仍约 64%、P9 1/4。

## 第二批第 3 项（累计第 10 项）：完整集合来源复验

上一项提交 `10da239`。新增 `research/source_request_set.py`，完整请求集合必须依次恢复
全部整日完成记录、重验原始来源和所有扫描位后再对账；不跳过 NO_SIGNAL，不选择部分
请求，不忽略 BLOCKED。上下文和规则审计在读取每日文件前执行，逐日消费限制内存范围。

新增 17 项测试；全量 717 passed、1 skipped，Ruff、mypy 139 文件、pip check 通过。集合编排测试明确 mock 每日
恢复接口，不冒充完整真实行情扫描；实际完成文件缺失会失败，没有声明回退路径。
旧声明型 API 保持兼容，必须显式使用来源验证入口；整体仍约 64%、P9 1/4。

## 第二批第 4 项（累计第 11 项）：来源与市场输入联合验收

上一项提交 `8089874`。扩展 `source_request_set.py`，市场数据验收及报告复用均先
逐日恢复原始扫描来源、重算完整请求集合，再验证全部 1m/Funding 响应字节。
来源错误在市场响应读取前传播；旧声明型报告不能替代缺失来源，历史报告不缓存放行。

新增 14 项测试；全量 731 passed、1 skipped，Ruff、mypy 140 文件、pip check 通过。测试明确模拟每日恢复接口，
市场 JSON 则实际读字节/哈希/规范化；无真实 API 访问或研究授权。
真实 BTC 四周期 3,937 行 pilot 和总语义报告哈希再次确认未变，整体仍约 64%、P9 1/4。

## 第二批第 5 项（累计第 12 项）：无验证接口 mock 的端到端验收

上一项提交 `7b82c08`。新增独立 `acceptance/`：构造完整四天合成 split，其中 DEV
第一天空池、第二天一个合约共 95 位；通过原始 ZIP/Parquet 实际计算 P3–P6、逐位/整日
复验、保存恢复、请求集合及 1m/Funding 验收，不模拟验证接口。正向后还会破坏合成 ZIP
验证已保存报告的再次复验。详情与命令见 `docs/p9-source-pipeline-acceptance.md`。

两项显式验收全部通过（351.00 秒）：完整 95 位为 94 个 NO_SIGNAL、1 个 accepted plan；
来源落盘/恢复、完整请求集合、526 根 1m 和 1 条 Funding 验收通过。随后实际修改临时
ZIP，旧报告复用正确失败。全部验证接口均为真实实现，来源 JSON 合计 3,111,141 bytes。
默认回归保持 731 passed、1 skipped；Ruff、mypy 143 文件和 pip check 通过。
不增加日常默认测试耗时，不运行真实策略、P7、账户或实盘。整体仍约 64%、P9 1/4。

第二批累计完成 5 项任务，新增 69 项默认回归测试及 2 项显式验收。真实研究仍需历史规则
证据与审核、实际多周期依赖/递归种子验证及受控行情采集；未自动解锁、未接入凭证或付费服务。

## 第三批第 1 项（累计第 13 项）：统一单扫描位入口

续接基线 `d21e929`，工作区干净。本批顺序计划：先实现正式单扫描位入口并回归，再将
完整合成验收接到该入口。只串联已有 P3–P6，不更改冻结规则或证据格式。

新增 `research/scan_slot.py`：完整扫描计划/参数/快照/规则审计、四周期一致性及精确 DEV
位置都在本入口行情读取前检查；与 P6 共用精确时刻历史规则选择，不能用日级审计覆盖
日内规则缺口。保留所有短路、NOT_READY、拒绝与 accepted 结果；不下载、落盘或执行 P7。

新增 24 项测试，六种合成来源场景逐字段比较原逐层计算结果；覆盖午夜归属、错序/错位、
篡改源文件、非 DEV、参数/Universe 变动、规则审核退回及日内断档。首轮两处测试夹具 UTC
字符串规范化问题已修正，16 项输入反例重跑通过。全仓 755 passed、1 skipped
（290.61 秒），Ruff、mypy 145 文件、pip check、diff 检查通过。冻结策略及依赖锁
原始字节哈希未变。整体仍约 64%、P9 1/4，种子和研究授权保持 false。

## 第三批第 2 项（累计第 14 项）：正式入口端到端接线

上一项提交 `722a07d`。合成验收的 `_source` 只负责构造显式历史前缀，P3–P6 统一调用
`compute_source_bound_scan_slot`；删除测试内重复串联，不修改合成价格、规则或预期结果。
两项显式验收全部通过（475.55 秒）：95 位仍为 94 个 NO_SIGNAL、1 个 accepted plan；
保存/恢复/请求集合、526 根合成 1m 与 1 条 Funding 通过。临时 ZIP 实际篡改后旧报告
复用正确失败；来源 JSON 仍为 3,111,141 bytes。没有替换验证接口。

默认回归最近为 755 passed、1 skipped；Ruff、mypy 145 文件、pip check 及 diff 检查
通过。此次只更换验收接线，没有新增默认测试或改变测试选择范围。

第三批两项已完成，共新增 24 项默认测试；整体仍为 9/14（约 64%）、P9 1/4。
下一实质步骤仍需可审核历史规则、实际多周期来源与递归种子材料；真实 DEV 16,440 个
成员日仍无合格规则，不能启动真实扫描、accepted 请求采集、P7 或 LOCKED_TEST。

## 第四批第 1 项（累计第 15 项）：整日历史来源一致性

续接基线 `6ce2981`，工作区干净。本批先修复整日缺少跨位历史来源一致性检查的问题，
再检查并接通跨日请求集的一致性。默认不允许隐式重置种子，未更改冻结策略公式。

新增 `ScanHistoryLineage` 并接入整日构建/复验：同一 symbol/interval 的历史起点固定，
已见月份使用同一完整来源凭证，新月份可延长前缀。通过各自原始文件校验的前缀也不能
混用不同起点或归档版本。检查器不缓存行情、不代替原始数据复验，也不批准种子。

新增 21 项测试，包含真实合成 ZIP 复验及四周期全部无信号位置的漂移反例。旧每日
编排夹具补全了历史时间几何声明，但仍明确模拟 P3–P6 边界，不冒充真实策略来源。
定向 57 项通过后补充两项反例，全仓 776 passed、1 skipped（211.66 秒）；Ruff、
mypy 146 文件、pip check 和 diff 检查通过。冻结策略与依赖锁原始哈希未变。
下一项接入跨日来源链共享；整体保持 9/14（约 64%）、P9 1/4，未执行真实研究。

## 第四批第 2 项（累计第 16 项）：跨日历史来源一致性

上一项提交 `7631678`。完整请求集、市场验收及旧报告复用每次创建新来源链，贯穿全部
整日恢复与逐位复验；拒绝跨日更换起点/月份凭证，不沿用历史“已通过”状态。
没有新增凭证格式、数据缓存、下载或研究授权；独立恢复仍默认检查整日内部一致性。

新增 12 项默认测试，定向 89 项全部通过；首次测试路径触发 Windows 长路径限制，已
缩短合成数据子目录名解决，没有改写生产存储规则。Ruff、mypy 146 文件、pip check
通过；全仓 788 passed、1 skipped（208.83 秒）。两项显式端到端验收通过（342.98 秒）：
94 个 NO_SIGNAL、1 个 accepted plan，526 根 1m 与 1 条 Funding 均通过。新增“原始前缀
有效但起点冲突”的恢复反例和原有 ZIP 篡改反例均正确拒绝；来源 JSON 本次为
3,110,476 bytes。仍为两个 DEV 日中一天空池、一天 95 位，不声称完成两个活跃日的全链路
容量验收；跨日共享/失败传播另由显式模拟整日恢复的定向测试验证。

第四批两项已完成，累计新增 33 项默认测试。当前测试没有审批任何真实种子或历史规则，
不执行真实 DEV 扫描、行情采集、P7、LOCKED_TEST 或下单。整体保持 9/14（约 64%）、
P9 1/4；实际研究仍需合格历史规则及完整来源/种子审核材料。

## 第五批第 1 项（累计第 17 项）：正式整日计算入口

续接基线 `fe9b6be`，工作区干净。本批继续按顺序实施：先将四周期前缀到整日结果的
编排移入正式入口，通过回归后，再将完整合成验收接到该入口。不增加策略或授权状态。

`compute_source_bound_scan_day` 在消费前缀流前执行整日上下文门禁；按时间、币种顺序
逐位检查四周期位置与来源链，计算 P3–P6，再重验整条来源。缺位、重复、错序、额外项、
来源漂移或计算错误均失败，只在全部位置通过后返回整日证据和 P6 来源；不写入文件。
空池严格要求空流，午夜保留前一天 Universe。只保留当前日来源，不声称全 DEV 性能就绪。

新增 24 项测试全部通过，编排测试明确 mock 逐位计算/复验边界；最后一位另使用真实
实现读取合成 ZIP/Parquet，并验证原始文件缺失会中断整日计算。全仓 812 passed、
1 skipped（219.91 秒）；Ruff、mypy 147 文件、pip check 和 diff 检查通过。
冻结策略与依赖锁原始哈希未变；下一项是正式整日入口的无验证接口 mock 合成验收。
整体仍为 9/14（约 64%）、P9 1/4；真实种子、规则和研究授权均未改变。
