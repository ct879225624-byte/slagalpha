# 开发续接记录：2026-09-03

用户要求：使用默认方案顺序开发，完成一项后自动进入下一项，每项汇报整体进度。
不重复询问普通工程选择；付费/凭证、历史规则审核、实盘等边界不因自动继续而改变。

## 第 1 项：扫描器原始 Candle 输入复验

新增 `research/candle_inputs.py`，校验下载/规范化凭证与固定归档身份，从原始 ZIP
重算完整 Candle，再逐字段核对 Parquet schema、元数据、值和入库时间。
防止同时伪造 Parquet 与其摘要后被接受；不把 Decimal 小数位差异当作值差异。
只访问项目内标准路径，拒绝符号链接和路径逃逸。原始数据未修改。

新增 26 项合成测试；本地真实 BTCUSDT 2024-01 四周期 3,937 行复验成功，重复报告哈希：
`f35e1e293a05278f0d605518ce91923f7e9451a96a266eb4625f2c824a62898e`。
全仓 557 passed、1 skipped（Windows symlink 权限），Ruff、mypy 121 文件、pip check 通过。

下一项：将完整连续历史前缀与扫描位绑定，严格控制多周期已收盘可见性并保留种子未验标记。
接口和限制见 `docs/p9-scanner-input-contract.md`。
整体保持 P0–P8 完成、P9 1/4、9/14（约 64%），未运行真实策略或实盘。
