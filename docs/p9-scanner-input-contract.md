# P9 扫描器行情输入边界

日期：2026-09-03。输入验证与后续扫描器接线，逐项开发；不批准历史规则、不运行真实策略。

## 1. 原始 ZIP → 规范化清单 → Parquet（已实现）

`research/candle_inputs.py::load_verified_candle_partition` 只接收 15m/1h/4h/1d 的
明确归档身份、下载凭证和规范化清单。检查官方固定 URL/文件名、同一 symbol/interval/月、
源哈希、行数和时间，并仅访问项目内的标准路径；拒绝逃逸和符号链接。

每次加载重新哈希原始 ZIP 和 Parquet；从 ZIP 重算规范化结果，核对清单全部统计，
再检查 Parquet 物理 schema、元数据、所有 Candle 权威值、收盘/来源标记及入库时间。
不会只信 Parquet 的哈希元数据，也不会因为伪造者同时更新文件哈希就接受修改的值。
复用既有 Decimal 规范化器，精确比较数值，不把 Arrow 的 18 位小数存储与原文小数位数
差异误判为不同值；不经过 float64 转换。

加载器返回从已验证 ZIP 重算、并与存储文件逐字段核对过的 Candle。声明凭证并非交易所
数字签名，仍需正式运行层绑定已冻结采集证据；单文件通过不证明完整 Universe/DEV 覆盖、
历史种子合格或策略可执行。

26 项合成定向测试通过。本地真实 BTCUSDT 2024-01 的四个周期复验通过：
15m 2,976 行、1h 744 行、4h 186 行、1d 31 行，合计 3,937 行。
两次验收报告一致：`f35e1e293a05278f0d605518ce91923f7e9451a96a266eb4625f2c824a62898e`。
原文件未改写，报告保存在本地忽略的数据目录；没有下载行情、运行指标或策略。

```powershell
.venv\Scripts\python.exe scripts/p9_candle_partition_pilot.py
.venv\Scripts\python.exe -m pytest -q tests/test_candle_inputs.py
```

## 2. 连续历史前缀（已实现）

`research/candle_history.py` 将明确的起点、周期、完整月份来源绑定到一个 DEV Universe
扫描位，按该周期最后完整收盘边界返回连续前缀。4H 的 08:15 只能看到 08:00 前收盘的
Candle；1D 的日内扫描只能看到当日午夜前收盘的 Candle。午夜归属遵循现有扫描清单。
缺月、重复/乱序月份、首尾缺 K 线及跨月接缝缺口全部拒绝；不填充、不排序修复。

源月文件先整体校验，再截取可见前缀；源文件在确认时刻之后的异常也可能保守阻断，
不会把未来行暴露给指标或策略。较早的 warm-up 历史可以跨 DEV 起点，但确认位必须在 DEV。
每个前缀保存完整来源和内容哈希；复用时重验原始字节。种子起点只是显式声明，
`history_seed_verified=false`、`research_authorized=false`，不能把它当成最早历史或已审核种子。

开发中修复直接重复规范化的入库时间不一致：旧 Parquet 被复用时从文件恢复原入库时间，
新文件在写入前规范到毫秒精度；不再创建带新入库时间的旧文件清单。既有文件未改写。
回归测试确认重复调用路径、文件字节和清单一致。历史旧清单按 Arrow 存储精度核对。

新增 25 项前缀测试并增强规范化幂等回归。全仓 582 passed、1 skipped；Ruff、
mypy 123 文件、pip check 通过；真实四周期 pilot 哈希保持不变。

## 3. P3 指标与结构证据（已实现）

`research/scan_features.py` 先绑定同一计划/候选/扫描清单，计划有 blockers 时在读取文件前
拒绝计算；随后重验前缀原始文件，复用 P3 计算指标、确认 Pivot 和 Zone。
Pivot 使用候选自己的 2/2 或 3/3 参数，不能借用另一候选的结果。
指标摘要使用 float64 精确十六进制值，warm-up NaN 显式编码为 null，禁止 Infinity。

证据保存原历史输入、参数、版本、指标摘要和完整 Pivot/Zone。复用时重新计算全部输出，
能识别自行重哈希的指标或遗漏结构；仅解析模型不能证明执行。
`history_seed_verified=false`、`strategy_executed=false`、`research_authorized=false` 保留。
这里的 strategy 指 P4 及以后的策略，不把 P3 特征计算当成完成策略扫描。

22 项合成测试验证参数绑定、未来行情不改变既有指标/结构、完整前缀不被 185 根截断、
原始字节篡改和自洽伪造输出。全仓 604 passed、1 skipped，Ruff、mypy 125 文件、pip check 通过。
未对真实行情运行 P3，没有写出真实策略证据。

## 4. 四周期 P4 Setup 重算（已实现）

`research/scan_setup.py` 要求 15m/1h/4h/1d 的证据严格同币种、同确认时刻、同 Universe、
同扫描清单和同候选；先重验全部原始来源与 P3，再复用 P4 的短路顺序。
有效结果标为 EVALUATED；合法历史不足单独标为 NOT_READY 并保留原因，不伪造 Setup，
更不生成 NO_SIGNAL。非法输入抛错，不被转换成历史不足。

15 项合成测试覆盖中性、压缩、日线阻断、有效 STANDARD 回踩、跨候选串接、文件改动、
伪造输出及历史不足。4H 185 根不足以覆盖最近 12 根已预热 SMA180（至少需要 191 根）；
这不改变 P4 规则，也不构成 ATR 递归历史充分性证明。
即使 P4 短路，绑定的其他周期来源仍需通过核验，保持来源集合一致。
`history_seed_verified=false`、`research_authorized=false` 保留；EVALUATED 只说明 P4 已计算。

全仓 619 passed、1 skipped；Ruff、mypy 127 文件、pip check 通过。没有真实数据策略运行。

## 5. P5 触发证据重算（已实现）

`research/scan_trigger.py` 从重验的 P4 与同一时点的 1H 指标冻结区域：SHALLOW 使用
SMA30–60，STANDARD 使用 SMA60–90，排序后传入 P5；不允许调用方手填区域。
15m 指标/Pivot/Zone 都来自对应前缀的重算。无效 P4 标为 SKIPPED_SETUP，历史不足
标为 NOT_READY，已评估但未触发保留完整失败原因，不把这些状态混成同一个结论。

10 项合成测试覆盖原始行情 → P3 → P4 → MA Reclaim 正向链路、冻结区域、状态区分和
篡改输出重验；所有正向价格/成交量都是明确的合成行情。
全仓 629 passed、1 skipped，Ruff、mypy 129 文件、pip check 通过；未计算真实市场触发。

## 6. P6 与有限 DEV 请求（已实现）

`research/scan_trade_plan.py` 将重验的 P5 接入 P6 Entry/Stop/TP。规则必须与当前
Universe/计划一致、在确认时刻 VERIFIED 且可交易；无效规则在行情文件读取前拒绝。
确认 K 线、ATR、Pivot/Zone 均由来源重算，tick size 使用对应历史规则。
只有 accepted plan 才生成有限 DEV 数据请求；规则须覆盖整个请求窗口，跨 DEV 边界直接
拒绝，不缩短窗口、不静默丢弃计划。区分无触发、历史不足、Entry/Stop 拒绝和 TP 拒绝。

16 项合成测试覆盖正向 526 根 1m 请求、规则失效、请求末端规则过期、跨 DEV、粗 tick
止损拒绝、前方障碍过近的 TP 拒绝和重哈希伪造。全仓 645 passed、1 skipped，Ruff、
mypy 131 文件、pip check 通过。没有请求下载、真实计算或交易授权。

## 7. 来源绑定的扫描记录（已实现）

`research/scan_records.py` 在重验原始文件与完整 P3–P6 链路之后，映射为现有
`DevScanRecord`，其上游哈希指向整份 P6 证据。合法无触发记为 NO_SIGNAL，价格计划
拒绝记为 REJECTED_PLAN，accepted 才携带计划。P4 或 P5 的 NOT_READY 均记为 BLOCKED，
以阶段前缀区分；输入异常直接传播，不会被转换成无信号。

原因码规范排序、去重，并保留 P4 的短路/回踩原因、P5 的 A/B 失败原因、P6 的止损/止盈
原因。`require_source_bound_scan_record` 重读来源并比较完整记录，而非只比上游哈希。
修改结果、原因、时刻、币种、证据引用或原始文件均不能复用；规则退回 UNVERIFIED 时拒绝。

这个显式入口只验证当前这一位。已有每日聚合器不自动调用它，整日/全 DEV 真实性仍需
逐位接线与完整覆盖验收，因此每日证据和请求集合的 `strategy_evidence_verified=false`
保持不变。逐位输出仍依赖未审核种子，不能独立作为研究通过凭证。

17 项新增合成测试；全仓 662 passed、1 skipped，Ruff、mypy 133 文件、pip check 通过。

## 8. 整日来源复验（已实现，2026-09-03 第二批）

`research/scan_days.py` 先由完整冻结快照/参数重建扫描清单，再用当前规则重新审计输入；
不匹配或受阻时不消费来源流。随后每个时间/symbol 位置必须有一份顺序正确的 P6 来源，
逐项调用原始 ZIP/Parquet 与 P3–P6 重算入口；缺位、重复、错序和额外项全部拒绝。
午夜确认仍使用前一天的 Universe，空池也必须提供恰好为空的来源流。

只在全部位置通过后返回整日记录；NOT_READY 仍记为 BLOCKED，输入异常原样传播。
重用整日结果会重新核对每条记录（含 NO_SIGNAL 的原因），不能只信摘要。
不更改旧声明型聚合器，必须显式调用新入口；种子未审核和研究未授权状态保持不变。

定向测试明确模拟逐位验证接口以检查 96 位调度，并在最后一位接入完整合成原始数据
复验；这不是 96 位均已跑真实原始来源的端到端报告。P3–P6 的完整计算由此前定向测试覆盖。

新增 17 项测试；全量 679 passed、1 skipped，Ruff、mypy 135 文件、pip check 通过。

## 9. 不可变来源保存与恢复（已实现）

`research/scan_storage.py` 先重验整日来源，再将完整逐位 P6 证据保存到
`data/manifests/scan_trade_plan_source/<hash>.json`，最后才发布
`data/manifests/source_scan_day/<hash>.json`。完成记录独立于旧声明型 daily evidence 目录，
避免把旧声明误认成已核验来源。相同内容可重试；既有不同字节拒绝覆盖。

中途失败可能留下来源文件，但没有完成记录就不能恢复为完成日。已有完成记录却缺少来源
时拒绝静默修补。加载检查规范 JSON、重复 key、完整模型和文件名身份；路径各层拒绝
symlink/junction。`read_declared_scan_source` 只读取声明，不证明行情仍有效；
`restore_source_bound_scan_day` 才会重新构建研究上下文并逐位重读原始行情、重算结果。

持久化测试显式 mock 整日重算边界，分别验证失败传播、写入顺序、中断恢复、不可覆盖和
路径限制；另验证自洽存储声明无法通过真实整日验证器。没有以存储测试冒充全日行情运行。
首版显式按日保存完整证据，无自动全 DEV 批量写入；尚未做跨位源元数据去重或生产容量验收。

新增 21 项测试；全量 700 passed、1 skipped，Ruff、mypy 137 文件、pip check 通过。

## 10. 来源驱动的完整请求集合（已实现）

`research/source_request_set.py` 将完整、有序的整日完成记录哈希接到来源恢复入口，
再复用既有请求集合契约。研究上下文与规则审计在打开来源日之前核对；日期集合必须
完整且无重复，每日返回值还须匹配选定哈希与精确位置。无信号和拒绝计划记录也会复验。

缺日、反序、损坏来源、BLOCKED、全无信号空集合及重新哈希后的手选请求子集均拒绝；
保留单请求的整个 DEV 窗口与历史规则约束。每次只恢复当前一天，不将全期来源模型
同时载入内存。显式新入口不改变旧声明型 API，调用旧入口仍不能声称已做来源复验。

测试明确 mock 已单独验收的每日恢复边界，验证全日调用、错误传播、重审规则与完整集合
绑定，并验证缺少实际完成文件时不会回退到声明。所有授权/种子限制保持不变。

新增 17 项测试；全量 717 passed、1 skipped，Ruff、mypy 139 文件、pip check 通过。

## 11. 来源链路与 1m/Funding 联合验收（已实现）

`source_request_set.py` 的市场验收入口先恢复所有来源日并重算请求集合，再调用原始
1m/Funding 字节校验器。保存的报告复用也遵循同样顺序：来源变化、市场响应变化、
规则退回未验证或手选子集，均拒绝；缺少来源完成记录时不能回退到旧声明型报告。

继续使用既有保守报告 schema，没有增加来源已永久验证或研究已授权的状态。
同一份报告本身无法证明调用了哪个入口，复用必须再次执行来源绑定入口；总语义门禁
仍要求来源重验，未因为返回旧格式报告就自动放行。正向测试模拟每日恢复边界，但
实际读取、哈希并规范化合成的 1m/Funding JSON；没有访问市场 API 或运行 P7。

新增 14 项测试；全量 731 passed、1 skipped，Ruff、mypy 140 文件、pip check 通过。

## 12. 无验证接口 mock 的端到端验收（已通过）

独立 `acceptance/test_source_pipeline.py` 使用完整合成四天 split，实际跑完两个 DEV 日
（一天空池、一天 95 位），不替换任何来源/策略/恢复/市场校验函数。最终 94 个 NO_SIGNAL、
1 个 accepted plan，完成来源保存恢复、请求集合及 526 根 1m / 1 条 Funding 验收。
之后修改临时 ZIP，旧报告复用被拒绝。两项验收均通过，耗时 351.00 秒。

默认回归仍为 731 passed、1 skipped；慢验收显式运行，不改变原测试选择范围。
命令、边界及合成数据说明见 `docs/p9-source-pipeline-acceptance.md`。

## 13. 单扫描位计算入口（已实现）

`research/scan_slot.py::compute_source_bound_scan_slot` 接收四份显式历史前缀及完整冻结
上下文，统一串联现有 P3 → P4 → P5 → P6，返回原有 `ScanTradePlanEvidence`，不引入
新的证据格式或修改策略公式。前缀读取/生成由调用方完成；本入口会重新读取原始来源。

在本入口读取行情和计算 P3 前，重建扫描清单、复核规则审计，并要求四周期按
15m/1h/4h/1d 排列、同币种/时刻/Universe/扫描清单，且属于 DEV 的精确扫描位。
日级审计并不证明日内规则连续性，因此还会复用 P6 的精确时刻历史规则检查。
午夜仍归属前一天 Universe；NOT_READY、无触发、拒绝和 accepted 保持原有区别。

调用只计算一个位置，不下载、不落盘、不执行 P7，不将历史起点声明改为已审核种子。
逐层复验仍然存在；不是缓存优化或全 DEV 调度器。

新增 24 项默认测试，全仓 755 passed、1 skipped。完整合成验收已改用本入口，两项
通过（475.55 秒），仍为 94 个 NO_SIGNAL、1 个 accepted plan，且通过保存恢复、
526 根 1m / 1 条 Funding 和原始来源篡改反例；详见端到端验收文档。

## 14. 整日历史来源一致性（已实现）

`ScanHistoryLineage` 在同一扫描清单内，为每个 symbol/interval 固定声明的历史起点，
并逐月绑定完整 `CandlePartitionSource` 内容哈希。后续位置允许增加新月份，但不能改变
已见月份的原始 ZIP、规范化内容、Parquet 或来源凭证。两份前缀各自有效，也不意味着
可以在同一天混用不同起点或不同版本的月份；否则递归 ATR 的输入会发生隐式变化。

整日构建和恢复复验均执行该检查，包含 NO_SIGNAL 与拒绝位置；不合格时不返回完成日。
检查器只保存起点与月份哈希，不缓存行情、不跳过逐位重算，也不审核种子。
跨日请求集合尚需共享同一条来源链，不能仅凭各日内部一致声称全期一致。

## 15. 后续边界

历史规则证据与实际种子边界验证齐备后，才进入真实 DEV 扫描与请求采集；不能靠更改
状态字段、忽略缺口或把合成正向测试换标签来解锁。

目前为逐位正确性优先的离线实现，层间复验会重复读取源文件；不是已完成的全 DEV 高效
调度器，不声称覆盖全部扫描位。请求集合仍保留 `strategy_evidence_verified=false`；
历史种子审核、完整来源覆盖和真实规则的阻断没有因单元测试通过而解除。

整体仍为 9/14（约 64%）、P9 1/4。新准备工作不计为真实研究完成。
