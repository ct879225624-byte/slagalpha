# Crypto Trading Copilot 实施计划

状态：P0–P8、P9.1、P9.2a 已完成；P9.2b 参数/输入哈希/语义门禁已完成，真实研究仍阻断
依据：`CRYPTO_AGENT_DEVELOPMENT_SPEC.md`  
制定日期：2026-08-26

## 1. 当前结论

当前仓库已完成并验收 P1 工程骨架、P2 历史数据闭环、P3 指标核心、P4 Setup、P5 Trigger、P6 Trade Plan、P7 事件驱动回测和 P8 历史数据与动态标的池。36 个月动态 Universe 已完成双轮重算；历史 tick/step 区间缺少权威证据时，Trade Plan/P7 会在读取 1m 数据前 Fail Closed。

开发必须按版本和验收闸门逐段推进。一次只实现一个可验证闭环；当前阶段失败时不得提前开发下一阶段，也不得用新增指标或扩大功能掩盖问题。

规格第 15 节的执行顺序与第 16、19 节存在冲突：第 15 节把 PostgreSQL、WebSocket 放在策略验证之前，但 V0.1 的目标是离线 Strategy Lab，且明确要求先完成 V0.1。为降低耦合和返工，实施顺序以版本验收为主：

1. V0.1 使用文件化研究数据、纯策略核心、CLI 和事件驱动回测。
2. V0.1 通过后，V0.2 才引入 PostgreSQL、Alembic、REST/WebSocket、FastAPI 和最小 Dashboard。
3. 策略核心从 V0.1 起保持与运行环境无关，V0.2/V0.3 只能调用它，不能复制一套实时逻辑。

## 2. 全程开发规则

每个阶段严格执行：

```text
冻结本阶段规则与输入输出
→ 先写失败测试或固定重放样本
→ 做最小实现
→ 运行单元测试、性质测试或重放
→ 检查未来函数、重复性和数据异常
→ 记录结果及遗留风险
→ 用户验收后进入下一阶段
```

通用质量闸门：

- 所有时间统一为 UTC，正式计算只读已收盘 K 线。
- 价格和交易规则使用 `Decimal`；指标计算可以使用浮点数，但进入价格计划时必须按 tickSize 明确舍入。
- 原始数据不可静默修复；缺失、重复、乱序和非法 OHLC 必须产生明确错误并 Fail Closed。
- 纯策略核心不得依赖网络、数据库、系统当前时间或随机数。
- 所有运行都保存策略版本、参数版本、数据清单、代码提交号和结果摘要。
- 同一输入重复运行必须生成相同信号、交易事件和统计结果。
- 测试集在参数冻结前不可查看；查看后再改规则必须提升策略版本。
- V1.0 前不接 API Key、不连接账户、不真实下单。

## 3. 开发前规则冻结（阶段 P0）

### 目标

把规格中的自然语言判断转成无歧义、可测试的决策表。此阶段只产出文档和测试样本设计，不实现业务代码。

### 必须明确的规则

1. `有效跌破 SMA90` 的精确定义。
2. SMA180 “接近水平”的观察窗口和阈值。
3. MA 顺序“频繁交换”的观察窗口和次数。
4. 价格“反复穿越整个均线带”的精确定义。
5. 1H 回踩的起止点，以及“最近 3 根回调 K 线”的周期和选取方式。
6. 普通 Pullback 与 Sweep 各自的量价规则；此前 20 根必须明确排除当前触发 K 线。
7. Trigger A 中“等价确认结构”是否删除；建议 V0.1 只保留突破上一根 15m 高/低点。
8. Trigger A、B 同时成立时的主触发优先级；建议 Sweep 优先，同时保存全部原因。
9. Pivot 合并成结构区域后的边界价、代表价和过期规则。
10. TP 支撑阻力识别、候选目标排序、冲突处理和价格舍入。
11. Signal Score 每一大项内部的确定性评分表。
12. 历史标的池的上线时间、下架合约和历史 quote volume 来源。

### 交付物

- `docs/strategy-rules-v0.1.md`
- `docs/decision-tables-v0.1.md`
- `docs/data-contracts-v0.1.md`
- 最少 20 个手工构造的边界场景清单，覆盖 LONG/SHORT、A/B Trigger、压缩、失效、过期和数据异常。

### 验收

- 每条正式信号规则都能回答“输入是什么、观察窗口是什么、边界是否包含、输出是什么”。
- 不再出现“有效”“接近”“频繁”“等价”“合理”等无法直接编码的词，或这些词已有量化定义。
- 用户确认规则冻结后才进入 P1。

## 4. V0.1 — Strategy Lab

### P1：工程骨架与契约测试

范围：只建立最小 Python 项目、配置、日志、测试和 CLI 空壳；不接网络、不建数据库、不写策略。

建议结构：

```text
src/slagalpha/
  domain/       # Candle、Signal、TradePlan 等不可变领域模型
  data/         # Binance 文件/REST Adapter，后续实现
  strategy/     # 指标、Setup、Trigger、风险计划纯函数
  backtest/     # 事件循环与成交模拟
  reporting/    # 统计与可复现报告
  cli.py
tests/
docs/
data/           # 默认不提交原始大文件
artifacts/      # 默认不提交回测产物
```

验收：测试、lint、类型检查和 CLI 帮助命令可运行；配置加载失败时给出明确错误；仓库不含密钥或大数据文件。

### P2：历史数据最小闭环

范围：先只处理 BTCUSDT、单一月份、单一周期，证明下载、checksum、解析、规范化和 Parquet 写入闭环；通过后再扩展到 15m/1H/4H/1D。

关键点：

- 数据源仅使用 Binance 官方 Public Data，REST 只用于缺口补齐和契约核验。
- 保存原始文件 checksum、下载 URL、下载时间、解析器版本和行数。
- Candle 唯一键为 `(exchange, symbol, interval, open_time)`。
- 校验时间网格、OHLC 关系、非负 quote volume、重复行和未收盘 K 线。

验收：损坏 checksum、缺 K 线、重复 K 线、乱序和非法 OHLC 均有测试；同一文件重复导入结果一致。

### P3：指标核心

范围：实现 SMA30/60/90/180、ATR14、quote-volume 统计、确认型 Pivot 和结构区域合并。

验收：

- 与独立参考计算结果一致。
- 指标 warm-up 行为明确。
- Pivot 只能在右侧 K 线已收盘后出现。
- 截断任意未来数据不改变更早时点的指标输出。

### P4：Setup 状态纯函数

范围：依次实现 4H 方向、1D 过滤、均线压缩、1H 回踩和状态转换；此阶段不计算 Entry/SL/TP。

实现顺序：LONG → 镜像 SHORT → 对称性测试。

验收：每条 P0 决策表都有测试；NEUTRAL、COMPRESSED、DAMAGED、RESET 必须 Fail Closed；多周期只读取各自最新已收盘 K 线。

### P5：15m Trigger

范围：先 Trigger A，再 Trigger B，最后实现同一行情去重与主触发归属。

验收：

- A/B 分别有正例、反例和边界测试。
- Sweep 不含未来函数。
- 同一 symbol/setup 同时满足 A/B 时只产生一个信号。
- 重放同一输入不会重复创建逻辑信号。

### P6：Entry / SL / TP 与评分

范围：实现价格计划、tickSize 舍入、ATR 风险范围、前方障碍过滤、最低 1:2 计划和只用于排序的 Score。

验收：

- LONG/SHORT 的舍入方向对交易者保持保守。
- Stop 距离超出 `0.5–2.0 ATR`、TP1 小于 1R、TP2 小于 2R 时拒绝信号。
- Score 不得绕过任何强制条件。
- 每个结果能输出完整 reason codes 和评分拆分。

### P7：事件驱动回测与成本模型

范围：按时间推进，不使用向量化偷看未来；加入 1m 成交路径、费用、滑点、Funding、TP1/TP2、保本和 TIME_EXIT。

开发顺序：

1. 单标的固定场景事件循环。
2. Entry 等待、INVALIDATED、EXPIRED、MISSED。
3. TP/SL/TIME_EXIT 生命周期。
4. 同一分钟多价位触及的不利结果规则。
5. Aggregate Trades 可用时的真实顺序重放。
6. 多标的运行与同时间信号分组。

验收：每种终态均有 golden fixture；理论价、模拟成交价、Fee、Slippage、Funding、Gross/Net R、MAE/MFE 可逐事件核对；中断后从事件日志重放结果不变。

### P8：数据范围扩展与历史标的池

范围：在单标的闭环通过后，扩展到至少 36 个月和历史动态标的池；不得先用今天 Top 30 代替历史池。

验收：每日标的池只使用当时已知信息；包含符合条件的后续下架合约；每个池有版本快照；单个异常标的不影响其他标的，但该标的当轮 Fail Closed。

### P9：研究、敏感度测试与冻结

严格顺序：

1. 按时间切分 50% 开发集、25% 验证集、25% 锁定测试集。
2. 仅在开发集按默认值逐项测试允许的少量参数。
3. 在验证集选择邻域稳定的候选，不做笛卡尔积搜索。
4. 冻结策略和参数版本。
5. 仅运行一次锁定测试集。
6. 同时输出无成本、基准成本、压力成本以及 Walk-forward 结果。

验收：无未来函数审计通过；重复运行哈希一致；报告覆盖规格要求的全部指标、分组、Block Bootstrap 置信区间及成本侵蚀；至少一个 Trigger 达到 V0.1 晋级条件，否则保持 V0.1 并标记失败或 `INCONCLUSIVE`。

## 5. 后续版本顺序（V0.1 通过后才启动）

### V0.2：Live Crypto Radar

1. PostgreSQL 数据模型与 Alembic 迁移。
2. Binance REST Adapter 与接口契约测试。
3. WebSocket 收盘事件、重连、去重、补洞和恢复校验。
4. 每日动态 Top 30 与版本快照。
5. 实时 Scanner 调用 V0.1 的同一策略核心。
6. FastAPI、SSE、最小 Dashboard。
7. 7 天连续稳定性验收和历史重放一致性比对。

### V0.3：Paper Trading Loop

1. 持久化订单生命周期状态机。
2. 1m 成交、Aggregate Trades、best bid/ask、Funding 与成本。
3. 重启恢复、幂等结算和冲突保护。
4. Paper Trade 页面与基础通知。
5. 14 天、至少 50 笔交易的工程闭环验收。

### V0.4：Analytics & Strategy Memory

完成分组统计、MAE/MFE、Score 校准、版本比较和每日/每周复盘。系统只能提出建议，不能自动改参数。

### V0.5：Personal Copilot

最后接入通知、信号解释、自然语言查询和研究建议。AI 服务与交易判断、扫描和 Paper Trading 解耦。

## 6. 首轮开发只做什么

本计划已由用户确认。P0 已按约束完成候选规则文档；用户验收 P0 后只启动 P1，不下载 36 个月数据、不实现策略或回测。

P0 完成并验收后，只启动 P1。之后每个阶段单独报告：

- 本阶段改了什么。
- 执行了哪些测试或重放。
- 验收条件是否全部通过。
- 发现的风险和待确认项。
- 是否建议进入下一阶段。

未经用户验收当前阶段，不跨越版本验收闸门。

## 7. 当前整体进度

| 范围 | 状态 | 完成度 |
|---|---|---:|
| 规划 | 已确认 | 100% |
| P0 规则冻结文档 | 已验收冻结 | 100% |
| P1 工程骨架 | 已验收 | 100% |
| P2 历史数据最小闭环 | 已验收 | 100% |
| P3 指标核心 | 已验收 | 100% |
| P4 Setup 上下文 | 已验收 | 100% |
| P5 15m Trigger | 已验收 | 100% |
| P6 Trade Plan 与评分 | 已验收 | 100% |
| P7 事件驱动回测 | 已验收 | 100% |
| P8 历史数据与动态标的池 | 已验收 | 100% |
| P9 研究、敏感度与冻结 | P9.1、P9.2a、取证/溯源/环境/参数与输入语义门禁完成；真实研究阻断 | 25% |
| V0.2 Live Radar | 未开始 | 0% |
| V0.3 Paper Trading | 未开始 | 0% |
| V0.4 Analytics | 未开始 | 0% |
| V0.5 Copilot | 未开始 | 0% |

按 P0–P9、V0.2、V0.3、V0.4、V0.5 共 14 个实施阶段粗略计，当前已完成 P0–P8 共 9/14（约 64%）；P8 内部完成 6/6，P9 内部完成 1/4。此前把 P0–P8 计为 10 个阶段是计数错误，本次修正不影响已完成工作。该比例只表示工程阶段，不代表策略已验证。

## 8. 当前未执行事项

- 未访问账户、API Key 或任何私有接口。
- P9 时间切分与输入审计已完成；尚未执行参数回测或锁定测试，未开始实时扫描、数据库或前端。
- DEV 规则缺口清单、本地证据验收和当前快照反例验收已完成；只属于 P9.2 输入准备，
  不计作真实敏感度研究完成。恢复记录见 `docs/development-checkpoint-2026-08-31.md`。
- DEV 运行记录、只读前检和本地环境版本锁已完成；首次本地 Git 基线已获用户授权，
  验收与跟踪边界见 `docs/git-baseline.md`。历史规则仍阻断，不以工程工具冒充策略验收。
- 10 个 DEV 参数版本已绑定冻结计划和策略规则；尚未完成正式执行器的语义与区间核验。
- DEV 文件内容哈希门禁已验证 12 类必需工件，另有 1 份附加诊断；显式空排除表已补齐并与 Universe 对齐；
  1m、Funding 仍缺失。语义门禁另确认多周期 normalization batch 不完整
  （27 个失败）。
- 27 个失败原始 ZIP 已逐个完成缺口审计；有限窗口无重叠，但 Wilder ATR 递归历史仍有
  18 个文件的依赖未解决，保持阻断。证据见 `docs/p9-normalization-gap-audit.md`。
- 缺口审计已作为可选诊断绑定到语义门禁；旧报告兼容，不移除 complete 要求。
- 29 个现有锁定版本的 wheel 已冻结文件哈希并完成离线 dry-run，语义门禁重读真实 wheel；
  未安装/升级依赖，见 `docs/p9-dependency-artifacts.md`。
